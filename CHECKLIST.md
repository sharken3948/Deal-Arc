# DealARC — Update Checklist
Run through this after every new feature or endpoint.

## Every Change
- [ ] README.md — endpoint table, project structure, stack
- [ ] app/docs/page.js — EndpointCard, request/response schema
- [ ] app/for-agents/page.js — agent API docs, code examples
- [ ] public/agent.json — machine-readable manifest
- [ ] .env.example — if new env variable added

## Major Changes Only
- [ ] CHANGELOG.md — if we keep one
